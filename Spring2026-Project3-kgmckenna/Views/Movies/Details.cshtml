@model Spring2026_Project3_kgmckenna.ViewModels.MovieDetailsViewModel

@{
    ViewData["Title"] = "Details";
}

@if (Model.Movie.Poster != null)
{
    <img src="data:image/jpeg;base64,@Convert.ToBase64String(Model.Movie.Poster)"
         alt="Movie Poster"
         style="max-width:300px; max-height:450px;" />
}
else
{
    <p>No poster uploaded.</p>
}

<h1>Details</h1>

<div>
    <h4>Movie</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Movie.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Movie.Title)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Movie.ImdbLink)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Movie.ImdbLink)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Movie.Genre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Movie.Genre)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Movie.Year)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Movie.Year)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Movie.Poster)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Movie.Poster)
        </dd>
    </dl>
    <h3>Actors in this Movie</h3>

    @if (Model.ActorNames.Any())
    {
        <ul>
            @foreach (var actor in Model.ActorNames)
            {
                <li>@actor</li>
            }
        </ul>
    }
    else
    {
        <p>No actors linked to this movie yet.</p>
    }

    <h3>Overall Sentiment: @Model.OverallSentiment</h3>

    <table class="table">
        <thead>
            <tr>
                <th>AI Review</th>
                <th>Sentiment</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.ReviewsWithSentiment)
            {
                <tr>
                    <td>@item.Review</td>
                    <td>@item.Sentiment</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?Movie.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
<div class="container">
    <div class="row mb-5">
        <div class="col-md-4 text-center">
            @if (Model.Movie.Poster != null)
            {
                <img src="data:image/*;base64,@Convert.ToBase64String(Model.Movie.Poster)"
                     class="detail-image"
                     alt="Movie Poster" />
            }
            else
            {
                <div class="image-placeholder">No Poster</div>
            }
        </div>

        <div class="col-md-8">
            <div class="detail-card">
                <h1>@Model.Movie.Title</h1>
                <hr />

                <p><strong>Genre:</strong> @Model.Movie.Genre</p>
                <p><strong>Year:</strong> @Model.Movie.Year</p>

                <p>
                    <strong>IMDb:</strong>
                    <a href="@Model.Movie.ImdbLink" target="_blank">View on IMDb</a>
                </p>

                <h4 class="mt-4">Actors</h4>

                @if (Model.ActorNames.Any())
                {
                    <ul>
                        @foreach (var actor in Model.ActorNames)
                        {
                            <li>@actor</li>
                        }
                    </ul>
                }
                else
                {
                    <p>No actors linked yet.</p>
                }

                <div class="mt-4">
                    <a asp-action="Edit" asp-route-id="@Model.Movie.Id" class="btn btn-primary me-2">Edit</a>
                    <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
                </div>
            </div>
        </div>
    </div>

    <div class="detail-card mb-4">
        <h2>AI Reviews</h2>
        <h5 class="text-muted">Overall Sentiment: @Model.OverallSentiment</h5>

        <table class="table table-striped mt-3">
            <thead class="table-dark">
                <tr>
                    <th>Review</th>
                    <th style="width: 150px;">Sentiment</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.ReviewsWithSentiment)
                {
                    <tr>
                        <td>@item.Review</td>
                        <td>
                            @if (item.Sentiment == "Positive")
                            {
                                <span class="badge bg-success">Positive</span>
                            }
                            else if (item.Sentiment == "Negative")
                            {
                                <span class="badge bg-danger">Negative</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Neutral</span>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>